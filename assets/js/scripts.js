/**
 * Main scripts file
 */

// Dependencies
var jQuery = require('jquery');

// Modules
var menu = require('./src/menu');
var fonts = require('./src/fonts-css-async');
var placeholder = require('./src/placeholder');
var lightbox = require('./src/lightbox');


jQuery(document).ready(function(){
	console.log('Init');
});